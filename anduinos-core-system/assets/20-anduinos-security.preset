# AnduinOS security defaults
#
# Remote access must be enabled explicitly by an administrator. Ubuntu's
# GNOME Control Center enables ssh.socket and disables ssh.service when the
# Secure Shell switch is turned on, and disables both when it is turned off.
disable ssh.service
disable ssh.socket
