stages:
  - lint
  - publish
  - meta

verify-ci-package-needs:
  stage: lint
  script:
    - python3 lib/verify-ci-package-needs.py
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH

verify-release-versions:
  stage: lint
  script:
    - python3 lib/verify-release-versions.py
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH

lint-all:
  stage: lint
  before_script:
    - dotnet tool update -g aiursoft.apkg.client --configfile ./nuget.config || dotnet tool install -g aiursoft.apkg.client --configfile ./nuget.config
    - export PATH="$PATH:$HOME/.dotnet/tools"
    - apkg --version
  script:
    - |
      failed=0
      for dir in */; do
        if ls "$dir"*.aosproj >/dev/null 2>&1; then
          echo "::group::Linting $dir"
          (cd "$dir" && apkg lint) || failed=1
          echo "::endgroup::"
        fi
      done
      exit $failed
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH != "master"

# GNOME puts X-GNOME-Settings-Panel entries in a hidden menu directory. ArcMenu
# builds its All Apps view from that menu tree, so standalone applications using
# either of these categories remain searchable but disappear from All Apps.
desktop-entry-policy:
  stage: lint
  script:
    - |
      offenders="$(grep -RIlE \
        --include='*.desktop' \
        '^(X-GNOME-Settings-Panel=|Categories=.*X-GNOME-(Settings-Panel|SystemSettings)(;|$))' \
        . || true)"
      if [ -n "$offenders" ]; then
        echo "Standalone applications must not use GNOME Control Center panel categories:" >&2
        echo "$offenders" >&2
        exit 1
      fi
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH

.publish:
  stage: publish
  before_script:
    - dotnet tool update -g aiursoft.apkg.client --configfile ./nuget.config || dotnet tool install -g aiursoft.apkg.client --configfile ./nuget.config
    - export PATH="$PATH:$HOME/.dotnet/tools"
    - apkg --version
  script:
    - cd "$PACKAGE_DIR"
    - |
      if [ "$CI_COMMIT_BRANCH" = "prod" ]; then
        APKG_SOURCE="https://apkg.aiursoft.com"
        APKG_KEY="$APKG_PROD_API_KEY"
      else
        APKG_SOURCE="https://apkg-dev.aiursoft.com"
        APKG_KEY="$APKG_DEV_API_KEY"
      fi
      apkg deploy \
        --source "$APKG_SOURCE" \
        --api-key "$APKG_KEY" \
        --skip-existing
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_COMMIT_BRANCH == "master"
    - if: $CI_COMMIT_BRANCH == "prod"

apkg:
  extends: .publish
  variables:
    PACKAGE_DIR: apkg

base-files:
  extends: .publish
  variables:
    PACKAGE_DIR: base-files

plymouth-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: plymouth-anduinos

anduinos-apt-config:
  extends: .publish
  needs:
    - anduinos-archive-keyring
    - anduinos-dracut-migration
    - anduinos-core-system
    - anduinos-btrfs-snapshots-manager
  variables:
    PACKAGE_DIR: anduinos-apt-config

anduinos-gdm3-wallpaper:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-gdm3-wallpaper

anduinos-apt-config-dev:
  extends: .publish
  needs:
    - anduinos-archive-keyring
    - anduinos-dracut-migration
    - anduinos-core-system
    - anduinos-btrfs-snapshots-manager
  variables:
    PACKAGE_DIR: anduinos-apt-config-dev

anduinos-appearance:
  extends: .publish
  needs:
    - anduinos-gdm3-wallpaper
  variables:
    PACKAGE_DIR: anduinos-appearance

anduinos-no-snapd:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-no-snapd

anduinos-fonts:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-fonts

anduinos-archive-keyring:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-archive-keyring

anduinos-bwrap-hack:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-bwrap-hack

anduinos-fluent-gtk-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-fluent-gtk-theme

anduinos-fluent-icon-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-fluent-icon-theme

anduinos-dconf-defaults:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: anduinos-dconf-defaults

anduinos-dconf-runtime:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-dconf-runtime

anduinos-deskmon:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-deskmon

anduinos-theme-sync:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-theme-sync

gnome-shell-extension-anduinos-loc:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-anduinos-loc

gnome-shell-extension-no-power-icon:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-no-power-icon

gnome-shell-extension-noti-bottom-right:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-noti-bottom-right

gnome-shell-extension-accent-gtk-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-accent-gtk-theme

gnome-shell-extension-accent-user-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-accent-user-theme

gnome-shell-extension-accent-icons-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-accent-icons-theme

gnome-shell-extension-appindicator-anduinos:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-appindicator-anduinos

gnome-shell-extension-arcmenu:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
    - anduinos-control-panel
  variables:
    PACKAGE_DIR: gnome-shell-extension-arcmenu

gnome-shell-extension-blur-my-shell:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-blur-my-shell

gnome-shell-extension-clipboard-indicator:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-clipboard-indicator

gnome-shell-extension-customize-ibus:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-customize-ibus

gnome-shell-extension-dash-to-panel-anduinos:
  extends: .publish
  needs:
    - anduinos-appearance
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-dash-to-panel-anduinos

gnome-shell-extension-desktop-icons-ng-anduinos:
  extends: .publish
  needs:
    - anduinos-appearance
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-desktop-icons-ng-anduinos

gnome-shell-extension-lockkeys:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-lockkeys

gnome-shell-extension-mediacontrols:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-mediacontrols

gnome-shell-extension-network-stats:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-network-stats

gnome-shell-extension-proxy-switcher:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-proxy-switcher

gnome-shell-extension-simple-weather:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-simple-weather

gnome-shell-extension-tiling-assistant:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: gnome-shell-extension-tiling-assistant

anduinos-wallpapers:
  extends: .publish
  needs:
    - anduinos-dconf-runtime
  variables:
    PACKAGE_DIR: anduinos-wallpapers

alsa-ucm-conf-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: alsa-ucm-conf-anduinos

firmware-sof-anduinos:
  extends: .publish
  needs:
    - alsa-ucm-conf-anduinos
  variables:
    PACKAGE_DIR: firmware-sof-anduinos

anduinos-theme:
  extends: .publish
  stage: meta
  needs:
    - anduinos-fluent-icon-theme
    - anduinos-fluent-gtk-theme
    - anduinos-gnome-extensions
    - anduinos-dconf-defaults
    - plymouth-anduinos
    - anduinos-wallpapers
    - anduinos-gdm3-wallpaper
    - anduinos-gnome-shell-locale
    - anduinos-fonts
    - anduinos-appearance
  variables:
    PACKAGE_DIR: anduinos-theme

anduinos-software-properties-common:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-software-properties-common

anduinos-software-properties-gtk:
  extends: .publish
  needs:
    - anduinos-software-properties-common
  variables:
    PACKAGE_DIR: anduinos-software-properties-gtk

anduinos-system-tweaks:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-system-tweaks

anduinos-kernel-parameters:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-kernel-parameters

anduinos-system-tweaks-server:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-system-tweaks-server

anduinos-templates:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-templates

anduinos-ufwall-gtk:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-ufwall-gtk

anduinos-swapcontrol-gtk:
  extends: .publish
  needs:
    - anduinos-swap-config
  variables:
    PACKAGE_DIR: anduinos-swapcontrol-gtk

anduinos-swap-config:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-swap-config

anduinos-btrfs-snapshots-manager:
  extends: .publish
  needs:
    - anduinos-secureboot-toolkit
  variables:
    PACKAGE_DIR: anduinos-btrfs-snapshots-manager

anduinos-why-ai:
  extends: .publish
  timeout: 3h
  variables:
    PACKAGE_DIR: anduinos-why-ai

anduinos-whisper-framework:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-whisper-framework

anduinos-whisper-gtk:
  extends: .publish
  needs:
    - anduinos-whisper-framework
  variables:
    PACKAGE_DIR: anduinos-whisper-gtk

anduinos-yubikey-manager:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-yubikey-manager

anduinos-why-placeholder:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-why-placeholder

anduinos-bash-guess-command:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-bash-guess-command

anduinos-xbox-controller-driver:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-xbox-controller-driver

anduinos-driver-center:
  extends: .publish
  needs:
    - anduinos-secureboot-toolkit
  variables:
    PACKAGE_DIR: anduinos-driver-center

anduinos-secureboot-toolkit:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-secureboot-toolkit

anduinos-gnome-shell-locale:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-gnome-shell-locale

anduinos-rime:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-rime

anduinos-dracut-migration:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-dracut-migration

anduinos-live-layers:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-live-layers

anduinos-live-settings:
  extends: .publish
  needs:
    - anduinos-live-layers
  variables:
    PACKAGE_DIR: anduinos-live-settings

anduinos-multimedia-codecs:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-multimedia-codecs

anduinos-installer-beta:
  extends: .publish
  needs:
    - anduinos-live-settings
    - anduinos-multimedia-codecs
    - anduinos-swap-config
  variables:
    PACKAGE_DIR: anduinos-installer-beta

# ── Onion-layer metapackages ─────────────────────────────────────────────
# anduinos-core-system: depends on the container, GRUB fonts, and system tweaks
# anduinos-desktop-core: depends on core-system
# anduinos-appstore:       Flatpak + GNOME Software (optional for servers)

anduinos-container:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-container

anduinos-grub-style:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-grub-style

anduinos-core-system:
  extends: .publish
  needs:
    - anduinos-container
    - anduinos-grub-style
    - anduinos-system-tweaks
  variables:
    PACKAGE_DIR: anduinos-core-system

anduinos-desktop-core:
  extends: .publish
  stage: meta
  needs:
    - anduinos-core-system
    - anduinos-session
    - firmware-sof-anduinos
    - alsa-ucm-conf-anduinos
    - anduinos-why-placeholder
    - anduinos-bash-guess-command
  variables:
    PACKAGE_DIR: anduinos-desktop-core

anduinos-appstore:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-appstore

# ── Meta-packages: must wait for their dependencies ──────────────────────

anduinos-gnome-extensions:
  extends: .publish
  stage: meta
  needs:
    - gnome-shell-extension-anduinos-loc
    - gnome-shell-extension-no-power-icon
    - gnome-shell-extension-noti-bottom-right
    - gnome-shell-extension-accent-gtk-theme
    - gnome-shell-extension-accent-user-theme
    - gnome-shell-extension-accent-icons-theme
    - gnome-shell-extension-appindicator-anduinos
    - gnome-shell-extension-arcmenu
    - gnome-shell-extension-blur-my-shell
    - gnome-shell-extension-clipboard-indicator
    - gnome-shell-extension-customize-ibus
    - gnome-shell-extension-dash-to-panel-anduinos
    - gnome-shell-extension-desktop-icons-ng-anduinos
    - gnome-shell-extension-lockkeys
    - gnome-shell-extension-network-stats
    - gnome-shell-extension-proxy-switcher
    - gnome-shell-extension-simple-weather
    - gnome-shell-extension-tiling-assistant
  variables:
    PACKAGE_DIR: anduinos-gnome-extensions

anduinos-session:
  extends: .publish
  needs:
    - anduinos-mimeapps
  variables:
    PACKAGE_DIR: anduinos-session

anduinos-mimeapps:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-mimeapps

firefox-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: firefox-anduinos

anduinos-exe-runner:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-exe-runner

anduinos-control-panel:
  extends: .publish
  needs:
    - anduinos-whisper-gtk
    - anduinos-driver-center
    - anduinos-appearance
    - anduinos-ufwall-gtk
    - anduinos-swapcontrol-gtk
    - anduinos-yubikey-manager
    - anduinos-btrfs-snapshots-manager
  variables:
    PACKAGE_DIR: anduinos-control-panel

anduinos-oobe:
  extends: .publish
  needs:
    - anduinos-appearance
    - anduinos-secureboot-toolkit
    - anduinos-driver-center
  variables:
    PACKAGE_DIR: anduinos-oobe

anduinos-desktop-apps:
  extends: .publish
  stage: meta
  needs:
    - anduinos-control-panel
    - anduinos-driver-center
    - anduinos-mimeapps
    - firefox-anduinos
    - anduinos-ufwall-gtk
    - anduinos-swapcontrol-gtk
    - anduinos-yubikey-manager
    - anduinos-exe-runner
    - anduinos-oobe
    - anduinos-btrfs-snapshots-manager
  variables:
    PACKAGE_DIR: anduinos-desktop-apps

# ── Ultimate meta-package ──────────────────────────────────────────────────

anduinos-desktop:
  extends: .publish
  stage: meta
  needs:
    - anduinos-desktop-core
    - anduinos-appstore
    - anduinos-apt-config
    - anduinos-apt-config-dev
    - anduinos-deskmon
    - anduinos-theme-sync
    - base-files
    - anduinos-theme
    - anduinos-templates
    - anduinos-software-properties-common
    - anduinos-no-snapd
    - anduinos-kernel-parameters
    - anduinos-swap-config
    - anduinos-desktop-apps
  variables:
    PACKAGE_DIR: anduinos-desktop
